[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "dsa-atlas"
version = "2.0.0"
description = "Tested reference implementations for DSA Atlas"
requires-python = ">=3.11"

[tool.setuptools]
packages = ["dsa_atlas"]
